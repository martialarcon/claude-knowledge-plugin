# CLAUDE.md — {{PROJECT_NAME}}

## Instrucciones obligatorias

1. Si la petición es ambigua, preguntar antes de responder.
2. Consultar el KB **antes** de leer código fuente.
3. El código prevalece sobre la doc en caso de conflicto.
4. No especular sobre código sin abrirlo.
5. Preguntar antes de `commit`/`push`.
6. Nunca indicar que algo ha sido generado por IA — en código, docs o git.
7. Cambios importantes requieren aprobación humana.

---

## Sistema

<!-- PROJECT-SPECIFIC: describe en 2-3 líneas qué hace el sistema. -->
{{PROJECT_NAME}}.

---

## Skills disponibles

| Skill | Comando | Propósito |
|---|---|---|
| Knowledge Agent | `/{{PROJECT_SLUG}}` | Analista del KB. Viabilidad, impacto, trazas, diagnóstico. Produce `handoff/{id}.yaml`. |
| Planner | `/planner` | Toma un handoff + ticket {{ISSUE_PREFIX}}-NNN y genera `plans/{id}.md`. No implementa. |

Flujo:
```
/{{PROJECT_SLUG}} analyze + /{{PROJECT_SLUG}} handoff {{ISSUE_PREFIX}}-NNN
       ▼
 handoff/{{ISSUE_PREFIX}}-NNN.yaml
       ▼
/planner {{ISSUE_PREFIX}}-NNN
       ▼
 plans/{{ISSUE_PREFIX}}-NNN.md
```

---

## Estructura KB

```
L0-system/       Arquitectura, entornos, branches
L1-modules/      Detalle por módulo
L2-domain/       Entidades de negocio
L3-flows/        Flujos entre módulos
L4-integrations/ Contratos API, dependencias externas
L5-issues/       Issues (open/resolved/info)
L6-decisions/    ADRs
L7-functional/   Especificaciones funcionales (fuente de verdad)
analysis/        Análisis generados
handoff/         Contexto KA → Planner
plans/           Planes de implementación
index/           keywords.json (routing)
```

---

## Issue tracker

- **Tracker**: {{ISSUE_TRACKER}}
- **Base URL**: {{ISSUE_TRACKER_URL}}
- **Prefijo**: `{{ISSUE_PREFIX}}-NNN`
- **Detalle**: `.claude/skills/planner/references/trackers/{{ISSUE_TRACKER}}.md`

El Planner **rechaza** generar plan sin un ID válido.

---

## Memoria operativa

- Preguntar siempre antes de `commit`/`push`.
- Nunca indicar generación por IA.
- Rellenar el resto de este archivo a medida que el proyecto crezca.
